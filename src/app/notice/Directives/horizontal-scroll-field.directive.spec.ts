import { HorizontalScrollFieldDirective } from './horizontal-scroll-field.directive';

describe('HorizontalScrollFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalScrollFieldDirective();
    expect(directive).toBeTruthy();
  });
});
