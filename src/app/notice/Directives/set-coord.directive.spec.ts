import { SetCoordDirective } from './set-coord.directive';

describe('SetCoordDirective', () => {
  it('should create an instance', () => {
    const directive = new SetCoordDirective();
    expect(directive).toBeTruthy();
  });
});
